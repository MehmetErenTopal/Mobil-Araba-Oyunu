import { defineConfig } from 'vite';

export default defineConfig({
  // base: './' ayarı, projenin GitHub Pages gibi alt dizinlerde (ör: github.com/kullanici/oyun-adi)
  // sorunsuz çalışmasını ve dosyaların doğru yollardan yüklenmesini sağlar.
  base: './',
});
